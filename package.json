{
  "name": "csv-linter",
  "version": "0.1.0",
  "description": "Lints CSV files and reports malformed rows with exact line and column numbers.",
  "type": "module",
  "bin": {
    "csv-linter": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/cli.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.5.3"
  }
}
